import React from 'react';
import { StyleSheet, View, Text } from 'react-native';

const NewTreeProposal = ({ navigation }) => {
  return (
    <View>
      <Text style={[styles.title]}>Hola Mundo</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default NewTreeProposal;
